# Theming

`dot_config/palette.json` is the single source of truth for the "warm-light" color scheme, generated by
[mini.hues](https://github.com/echasnovski/mini.hues) from the Neovim config. Every other app's colors are
manually kept in sync with it — there's no shared build step, so if you regenerate the palette in Neovim,
the files below need updating by hand (or ask Claude to do it, quoting the new `palette.json`).

## Consumers

| App | File | Notes |
|---|---|---|
| Neovim | `dot_config/nvim/plugin/30_mini.lua` | Source of the palette (`mini.hues.setup`) |
| tmux | `dot_config/tmux/tmux.conf` | Status bar / pane border colors |
| WezTerm | `dot_config/wezterm/wezterm.lua` | `config.colors` (ANSI 16-color table) |
| Discord (via [Vencord](https://vencord.dev/)) | `AppData/Roaming/Vencord/themes/nvim-warm-light.theme.css` | See below |
| Windows | registry (accent color) + solid wallpaper | See below |

## Discord (Vencord)

Requires [Vencord](https://vencord.dev/) installed (a Discord client mod) with `useQuickCss` enabled.

The theme file targets Discord's **"visual refresh"** UI, which uses variables like `--background-base-lowest`,
`--text-default`, `--brand-500`, scoped under `.visual-refresh.theme-light`. This is a different, newer scheme
than the classic `--background-primary` variables — if a future Discord redesign changes the scheme again and
colors stop applying, the fastest way to find the current variable names is to inspect an actively-maintained
community theme's compiled CSS, e.g.:

```
https://catppuccin.github.io/discord/dist/catppuccin-latte.theme.css
```

`run_configure-vencord-theme.ps1.tmpl` runs on every `chezmoi apply` (Windows only) and ensures
`nvim-warm-light.theme.css` is the sole enabled theme in Vencord's `settings.json`. It's a no-op if Vencord
isn't installed yet.

**Manual fallback**: Discord Settings → Vencord → Themes → toggle "Nvim Warm Light" on (and other themes off),
make sure "Load Vencord CSS" (`useQuickCss`) is on. `Ctrl+R` reloads Discord to pick up CSS changes.

## Windows (accent color + wallpaper)

Windows doesn't support arbitrary theming the way terminal apps or Discord do. What's actually achievable
without third-party tools:

- **Accent color** (Start menu, taskbar highlights, title bars, selection highlights) — driven by the
  palette's `accent` color, via registry keys under `HKCU:\Software\Microsoft\Windows\DWM` and
  `HKCU:\...\Explorer\Accent`.
- **Light/dark mode** — forced to light, matching the palette.
- **Wallpaper** — a solid fill of the palette's `bg` color (no interesting texture, just a flat match).

**Not achievable** without installing a shell patcher: File Explorer's window background is hardcoded
white/light-gray by the Fluent design system — there's no registry key for it. Tools like
[ExplorerPatcher](https://github.com/valinet/ExplorerPatcher) or
[StartAllBack](https://www.startallback.com/) can go further, but that's a much bigger, harder-to-reverse
change (system-level shell patching) and hasn't been set up here.

`run_onchange_configure-windows-accent.ps1.tmpl` applies all of the above, reading colors directly from
`dot_config/palette.json` at runtime. It only re-runs when `palette.json`'s content changes (tracked via a
hash embedded in the script), since it restarts `explorer.exe` to apply the accent color immediately — not
something that should happen on every unrelated `chezmoi apply`.

**Manual fallback**: Settings → Personalization → Colors → pick a custom accent color matching
`palette.json`'s `accent` value; Settings → Personalization → Background → solid color matching `bg`.
